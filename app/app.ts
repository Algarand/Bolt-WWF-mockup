import { Application } from '@nativescript/core';

// Start the application immediately
Application.run({ moduleName: 'app-root' });

// We'll initialize Firebase later when we have proper credentials
// firebase.initializeApp({...})